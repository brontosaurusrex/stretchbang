#!/bin/bash

# up
# update only if it was updated more than N minutes ago

N="5"

if [ -z "$(find -H /var/lib/apt/lists -maxdepth 0 -cmin -$N)" ]; then

    echo "mkay"
    sudo apt-get update && sudo apt-get dist-upgrade -y
    
else

    echo "nope"

fi
