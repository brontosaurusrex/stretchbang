#!/bin/bash
 
data="chuck-norris-jokes.txt"

c="0"
unlock="60" 
 
until 
    seen 250 chuck "$(shuf -n 1 $data)"

    do 
        # lock prevention
        (( c++ ))
        if (( c == unlock )); then 
             >&2 echo "lock prevented"
            exit
        fi
    done
